# ── Render TA Server Requirements ──────────────────────────────────────────
# Install: pip install -r requirements_ta_server.txt

# Web server
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# TA module dependencies (copied from TechnicalAnalysis_v9_6.py internals)
aiohttp==3.9.0
requests==2.31.0
urllib3>=2.0.0
Pillow>=10.0.0
pytz==2024.1
feedparser==6.0.10
numpy>=1.26.0
scipy>=1.12.0

# Optional: speeds up chart rendering
matplotlib>=3.8.0

# Telegram client (TA module imports some Telegram types internally)
python-telegram-bot==20.7
